import http from 'node:http';
import youtubedl from 'youtube-dl-exec';
const PORT=Number(process.env.PORT||10000),MUX='https://ivy-ytmux-backend.onrender.com',DEFAULT_ID='AjSxpi8E9WE',CHANNEL_HANDLE='KhoaiLangThang',ADDON_ID='community.ivy.youtube.khoailangthang',CATALOG_ID='ivy-khoai-lang-thang';
const videoMap=new Map(),qc=new Map(),sc=new Map();let loading=false,loaded=0,ready=false,refreshPromise=null;
videoMap.set(DEFAULT_ID,{id:DEFAULT_ID,title:'Hành trình xuyên đảo núi lửa Iceland P5 - Du lịch Châu Âu'});
function send(r,c,b){r.writeHead(c,{'content-type':'application/json; charset=utf-8','cache-control':'no-store','access-control-allow-origin':'*'});r.end(JSON.stringify(b))}
function meta(v){return{id:`yt:${v.id}`,type:'movie',name:v.title||`YouTube ${v.id}`,poster:`https://i.ytimg.com/vi/${v.id}/hqdefault.jpg`,background:`https://i.ytimg.com/vi/${v.id}/maxresdefault.jpg`,description:v.description||'Khoai Lang Thang / Food & Travel',genres:['YouTube','Khoai Lang Thang','Travel'],releaseInfo:'YouTube'}}
async function scan(t,target){let d=await youtubedl(`https://www.youtube.com/@${CHANNEL_HANDLE}/${t}`,{dumpSingleJson:true,flatPlaylist:true,skipDownload:true,noWarnings:true,ignoreErrors:true});for(const e of d?.entries||[])if(/^[\w-]{11}$/.test(e?.id||''))target.set(e.id,{id:e.id,title:e.title,description:e.description})}
async function refresh(force=false){if(refreshPromise)return refreshPromise;if(!force&&ready&&Date.now()-loaded<216e5)return;refreshPromise=(async()=>{loading=true;try{const next=new Map();await scan('videos',next);await scan('shorts',next);if(next.size<2)throw Error(`incomplete catalog ${next.size}`);videoMap.clear();for(const [k,v] of next)videoMap.set(k,v);loaded=Date.now();ready=true;console.log('[CATALOG-READY]',videoMap.size)}catch(e){console.log('[CATALOG-REFRESH-FAIL]',String(e),'keeping',videoMap.size);if(videoMap.size>1)ready=true;throw e}finally{loading=false;refreshPromise=null}})();return refreshPromise}
async function transport(id){let c=qc.get(id);if(c&&c.e>Date.now())return c.v;try{let r=await fetch(`${MUX}/qualities/${id}.json`,{signal:AbortSignal.timeout(90000)}),txt=await r.text(),v;try{v=JSON.parse(txt)}catch{throw Error(`backend non-json HTTP ${r.status}: ${txt.slice(0,80)}`)}if(!r.ok)throw Error(`HTTP ${r.status}`);qc.set(id,{v,e:Date.now()+(v.complete?216e5:15000)});return v}catch(e){console.log('[TRANSPORT-FAIL]',id,String(e));return null}}
async function source(id){let c=sc.get(id);if(c&&c.e>Date.now())return c.v;try{let d=await youtubedl(`https://www.youtube.com/watch?v=${id}`,{dumpSingleJson:true,skipDownload:true,noWarnings:true,ignoreErrors:true,extractorArgs:'youtube:player_client=web'});let hs=[...new Set((d?.formats||[]).filter(f=>f&&f.vcodec&&f.vcodec!=='none'&&Number(f.height)>0).map(f=>Number(f.height)))].sort((a,b)=>b-a);let v={ok:hs.length>0,qualities:hs,max:hs[0]||null};sc.set(id,{v,e:Date.now()+216e5});console.log('[SOURCE-YOUTUBE]',id,hs.join(','));return v}catch(e){console.log('[SOURCE-YOUTUBE-FAIL]',id,String(e));return{ok:false,qualities:[],max:null}}}
const manifest={id:ADDON_ID,version:'11.1.4',name:'Ivy ❤️ Khoai Lang Thang',description:'Full catalog is loaded atomically before it is exposed; playback resolver is isolated from catalog.',resources:['catalog','meta','stream'],types:['movie'],catalogs:[{type:'movie',id:CATALOG_ID,name:'Khoai Lang Thang • Full Channel'}],idPrefixes:['yt:']};
http.createServer(async(req,res)=>{try{if(req.method==='OPTIONS'){res.writeHead(204,{'access-control-allow-origin':'*'});return res.end()}const p=decodeURIComponent(new URL(req.url,'http://x').pathname);if(p==='/')return send(res,200,{service:manifest.name,version:manifest.version,videos:videoMap.size,ready,loading});if(p==='/manifest.json')return send(res,200,manifest);if(p===`/catalog/movie/${CATALOG_ID}.json`){try{if(!ready)await refresh(true);else void refresh();}catch(e){if(videoMap.size<=1)return send(res,503,{metas:[],error:'catalog loading failed; refusing partial catalog'});}return send(res,200,{metas:[...videoMap.values()].map(meta)})}let m=p.match(/^\/meta\/movie\/yt:([\w-]{11})\.json$/);if(m)return send(res,200,{meta:meta(videoMap.get(m[1])||{id:m[1]})});m=p.match(/^\/stream\/movie\/yt:([\w-]{11})\.json$/);if(m){const id=m[1];const [src,tr]=await Promise.all([source(id),transport(id)]);const arr=[];const tq=new Map((tr?.qualities||[]).map(x=>[x.quality,x]));if(src.ok){const genuine=src.qualities.filter(q=>tq.has(q));if(genuine.length){const top=genuine[0],x=tq.get(top);arr.push({name:`AUTO • ${top}p`,title:`Source verified ${top}p • ${x.codec}`,url:`${MUX}/stream/${id}/${top}.mp4`,behaviorHints:{notWebReady:true}});for(const q of genuine){const z=tq.get(q);arr.push({name:`Ivy YouTube • ${q}p`,title:`Source verified ${q}p • ${z.codec}`,url:`${MUX}/stream/${id}/${q}.mp4`,behaviorHints:{notWebReady:true}})}console.log('[AUTO-SOURCE]',id,src.max)}}if(!arr.length){const safe=[360,240,144].filter(q=>tq.has(q));for(const q of safe){const z=tq.get(q);arr.push({name:`Ivy YouTube • ${q}p`,title:`PLAYABLE FALLBACK • source max not verified • ${z.codec}`,url:`${MUX}/stream/${id}/${q}.mp4`,behaviorHints:{notWebReady:true}})}console.log('[SOURCE-BLOCKED-FALLBACK]',id,safe.join(','))}return send(res,200,{streams:arr})}return send(res,404,{error:'not found'})}catch(e){console.log('[ERROR]',String(e));return send(res,502,{error:String(e)})}}).listen(PORT,'0.0.0.0',()=>{console.log('[START v11.1.4]',PORT);void refresh(true).catch(()=>{})});
