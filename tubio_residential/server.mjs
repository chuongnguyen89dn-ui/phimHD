import http from 'node:http';
const PORT=Number(process.env.PORT||10000), TEST='XwY9ysGXkI4';
const ORIGIN='https://ivy-tubio-residential-test.onrender.com', MUX='https://ivy-ytmux-backend.onrender.com';
const manifest={id:'community.ivy.nuvio.residential.clean.v5',version:'5.1.1',name:'Nuvio Residential Playback Test',description:'Instant proven baseline first while higher adaptive qualities remain manual tests',resources:['catalog','meta','stream'],types:['movie'],catalogs:[{type:'movie',id:'residentialtest',name:'Nuvio Residential Test'}]};
const meta={id:`res:${TEST}`,type:'movie',name:'Residential Nuvio Test - Khoai Lang Thang',poster:`https://i.ytimg.com/vi/${TEST}/hqdefault.jpg`,description:'AUTO uses the proven instant direct path. Higher qualities remain manual until instant adaptive playback is fixed.'};
function json(res,obj,status=200){res.writeHead(status,{'content-type':'application/json','access-control-allow-origin':'*','cache-control':'no-store'});res.end(JSON.stringify(obj))}
const server=http.createServer(async(req,res)=>{try{const raw=new URL(req.url,'http://x').pathname,path=decodeURIComponent(raw);console.log('[REQ]',req.method,raw,'=>',path);if(path==='/manifest.json')return json(res,manifest);if(path==='/catalog/movie/residentialtest.json')return json(res,{metas:[meta]});if(path===`/meta/movie/res:${TEST}.json`)return json(res,{meta});if(path===`/stream/movie/res:${TEST}.json`){const streams=[{name:'AUTO • Instant',title:'AUTO • Proven instant playback',url:`${MUX}/stream/${TEST}/360.mp4`,behaviorHints:{notWebReady:true}},{name:'YouTube 1080p • TEST',title:'Manual test • 1080p adaptive',url:`${MUX}/stream/${TEST}/1080.mp4`,behaviorHints:{notWebReady:true}},{name:'YouTube 720p • TEST',title:'Manual test • 720p adaptive',url:`${MUX}/stream/${TEST}/720.mp4`,behaviorHints:{notWebReady:true}},{name:'YouTube 480p • TEST',title:'Manual test • 480p adaptive',url:`${MUX}/stream/${TEST}/480.mp4`,behaviorHints:{notWebReady:true}},{name:'YouTube 360p',title:'Manual • proven direct 360p',url:`${MUX}/stream/${TEST}/360.mp4`,behaviorHints:{notWebReady:true}}];console.log('[STREAMS-SAFE]',TEST,'auto=360-direct','hq=manual-test');return json(res,{streams})}if(path==='/health')return json(res,{ok:true,version:'5.1.1',autoQuality:360,autoReason:'proven-direct-baseline',manualTests:[1080,720,480]});return json(res,{error:'not found',path},404)}catch(e){console.error('[ERR]',String(e));json(res,{error:String(e)},502)}});
server.listen(PORT,'0.0.0.0',()=>console.log('[START v5.1.1]',PORT,`${ORIGIN}/manifest.json`));